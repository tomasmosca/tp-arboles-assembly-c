#include <stdio.h>
#include <stdlib.h>
#include "arbol_binario.h"

struct arbol_binario crearArbolB(int valor, struct arbol_binario *izq, struct arbol_binario *der);
struct arbol_binario buscarMin(struct arbol_binario *arbol);
void eliminarTodos(struct arbol_binario *arbol, int valor);

int main(int argc, char **argv)
{
    return 0;
}