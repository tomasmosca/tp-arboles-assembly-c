#include <stdio.h>
#include <stdlib.h>
#include <arbol_binario.h>

struct arbol_binario crearArbolB(int valor, arbol_binario *izq, arbol_binario *der);
struct arbol_binario buscarMin(arbol_binario *arbol);
void eliminarTodos(arbol_binario *arbol, int valor);

int main(int argc, char **argv)
{
    return 0;
}